#!/bin/bash

# Previous locations.
rm --force --recursive /home/*/{Documents,Documenten}/kz

if [[ $(lsb_release --id --short) = Debian ]]; then
    snap install core
    systemctl enable --now snapd.socket
    systemctl restart snapd.seeded.service
fi

true
